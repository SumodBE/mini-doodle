type: object
required:
  - id
  - email
  - timezone
  - createdAt

properties:
  id:
    type: string
    format: uuid

  email:
    type: string
    format: email

  timezone:
    type: string
    example: Asia/Kolkata

  createdAt:
    type: string
    format: date-time